name: service
description: "Service interfaces and registry for pluggable architecture."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.5.0

dependencies:
  app_logger:
    path: ../app_logger
  storage:
    path: ../storage
