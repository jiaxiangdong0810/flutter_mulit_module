name: base_ui
description: "Base UI components - BasePage, BaseDialog, etc."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  service:
    path: ../../base/service

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  logger:
    path: ../../base/logger

flutter:
